package oop.utils;

/**
 * The UserSessionObserver interface defines a method to be called when the user session changes.
 */
public interface UserSessionObserver {
    /**
     * This method is called when the user session changes.
     */
    void update();

}